# AGENTS.md instructions for /Users/emrecb/Documents/GitHub/ShotBrief

- If I ask a question, inspect the code and always answer first. For prompts that are questions, only review the code; never start making changes or taking action.
- If I ask to "start the project", "start it in the DOM", or make a similar request, first check whether a terminal/dev server is already running in the background. If not, start the project with `npm run dev`.
- After the dev server starts, or if it is already running, open the available localhost URL in the Codex in-app browser. If port 3000 is busy, use the new port reported by Next.js.
